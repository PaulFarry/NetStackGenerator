// generated by a tool
import React, { Component } from 'react';
import {TableCell, TableRow} from "@mui/material";
{{#if UnderlyingType.IsReferenceData}}
import { ILoginState, UserConsumer, isInRole } from '../../../api-authorization/UserContext';
{{/if}}

export class {{cs_name Name}}Header extends Component<any, any> {

    render() {
    return (<TableRow>
                {{#each ListFields}}
				<TableCell>{{DisplayName}}</TableCell>
				{{/each}}
				{{#if UnderlyingType.IsAttachmentWithThumbnail}}
				<TableCell></TableCell>
                {{/if}}
				<TableCell></TableCell>
            </TableRow>);
    }
}
